/*DROP TABLE TBL_Cidade CASCADE CONSTRAINTS;
DROP TABLE TBL_Regiao CASCADE CONSTRAINTS;
DROP TABLE TBL_SubPrefeitura CASCADE CONSTRAINTS;
DROP TABLE TBL_Temperatura CASCADE CONSTRAINTS;
DROP TABLE TBL_Temperatura CASCADE CONSTRAINTS;
DROP TABLE TBL_Umidade CASCADE CONSTRAINTS;
DROP TABLE TBL_Ventania CASCADE CONSTRAINTS;
DROP TABLE TBL_Precipitacao CASCADE CONSTRAINTS;
DROP TABLE TBL_Nuvem CASCADE CONSTRAINTS;
DROP TABLE TBL_Previsao CASCADE CONSTRAINTS;
DROP TABLE TBL_Previsao_Horaria CASCADE CONSTRAINTS;
DROP TABLE TBL_Historico_Meteorologico CONSTRAINTS;

--DROP SEQUENCE falta_add;
--DROP SEQUENCE falta_add;
--DROP SEQUENCE falta_add;*/

CREATE TABLE TBL_Cidade (
    Id      NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    Nome    VARCHAR2(50) NOT NULL,
    SG_Estado  CHAR(2) NOT NULL
);

COMMENT ON COLUMN TBL_Cidade.Id IS
    'Esta coluna irá receber o Id da cidade e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_Cidade.Nome IS
    'Esta coluna irá receber o Nome da cidade e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_Cidade.SG_Estado IS
    'Esta coluna irá receber a sigla do estado da cidade e seu conteúdo é obrigatório.';


CREATE TABLE TBL_Regiao (
    Id         NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    Nome       VARCHAR2(50) NOT NULL,
    Cidade_Id  NUMBER NOT NULL,
    FOREIGN KEY (Cidade_Id) REFERENCES TBL_Cidade(Id)
);

COMMENT ON COLUMN TBL_Regiao.Id IS
    'Esta coluna irá receber o Id da região e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_Regiao.Nome IS
    'Esta coluna irá receber o nome da região e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_Regiao.Cidade_Id IS
    'Esta coluna irá receber o id da cidade e seu conteúdo é obrigatório.';


CREATE TABLE TBL_SubPrefeitura (
    Id        NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    Nome      VARCHAR2(50) NOT NULL,
    Macro_Id  NUMBER NOT NULL,
    FOREIGN KEY (Macro_Id) REFERENCES TBL_Regiao(Id)
);

COMMENT ON COLUMN TBL_SubPrefeitura.Id IS
    'Esta coluna irá receber o Id da subprefeitura e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_SubPrefeitura.Nome IS
    'Esta coluna irá receber o Nome da subprefeitura e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_SubPrefeitura.Macro_Id IS
    'Esta coluna irá receber o Id da região e seu conteúdo é obrigatório.';


CREATE TABLE TBL_Temperatura (
    Id     NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    Data   DATE NOT NULL,
    Dia_1  FLOAT(3),
    Dia_2  FLOAT(3),
    Dia_3  FLOAT(3),
    Dia_4  FLOAT(3),
    Dia_5  FLOAT(3),
    Dia_6  FLOAT(3),
    Dia_7  FLOAT(3)
);

COMMENT ON COLUMN TBL_Temperatura.Id IS
    'Esta coluna irá receber o Id da temperatura registrada e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_Temperatura.Data IS
    'Esta coluna irá receber o Id da temperatura registrada e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_Temperatura.Dia_1 IS
    'Esta coluna irá receber a previsão da temperatura um dia após a data registrada.';

COMMENT ON COLUMN TBL_Temperatura.Dia_2 IS
    'Esta coluna irá receber a previsão da temperatura dois dias após a data registrada.';

COMMENT ON COLUMN TBL_Temperatura.Dia_3 IS
    'Esta coluna irá receber a previsão da temperatura três dias após a data registrada.';

COMMENT ON COLUMN TBL_Temperatura.Dia_4 IS
    'Esta coluna irá receber a previsão da temperatura quatro dias após a data registrada.';

COMMENT ON COLUMN TBL_Temperatura.Dia_5 IS
    'Esta coluna irá receber a previsão da temperatura cinco dias após a data registrada.';

COMMENT ON COLUMN TBL_Temperatura.Dia_6 IS
    'Esta coluna irá receber a previsão da temperatura seis dias após a data registrada.';

COMMENT ON COLUMN TBL_Temperatura.Dia_7 IS
    'Esta coluna irá receber a previsão da temperatura sete dias após a data registrada.';


CREATE TABLE TBL_Umidade (
    Id     NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    Data   DATE NOT NULL,
    Dia_1  FLOAT(3),
    Dia_2  FLOAT(3),
    Dia_3  FLOAT(3),
    Dia_4  FLOAT(3),
    Dia_5  FLOAT(3),
    Dia_6  FLOAT(3),
    Dia_7  FLOAT(3)
);

COMMENT ON COLUMN TBL_Umidade.Id IS
    'Esta coluna irá receber o Id da Umidade registrada e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_Umidade.Data IS
    'Esta coluna irá receber o Id da Umidade registrada e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_Umidade.Dia_1 IS
    'Esta coluna irá receber a previsão da Umidade um dia após a data registrada.';

COMMENT ON COLUMN TBL_Umidade.Dia_2 IS
    'Esta coluna irá receber a previsão da Umidade dois dias após a data registrada.';

COMMENT ON COLUMN TBL_Umidade.Dia_3 IS
    'Esta coluna irá receber a previsão da Umidade três dias após a data registrada.';

COMMENT ON COLUMN TBL_Umidade.Dia_4 IS
    'Esta coluna irá receber a previsão da Umidade quatro dias após a data registrada.';

COMMENT ON COLUMN TBL_Umidade.Dia_5 IS
    'Esta coluna irá receber a previsão da Umidade cinco dias após a data registrada.';

COMMENT ON COLUMN TBL_Umidade.Dia_6 IS
    'Esta coluna irá receber a previsão da Umidade seis dias após a data registrada.';

COMMENT ON COLUMN TBL_Umidade.Dia_7 IS
    'Esta coluna irá receber a previsão da Umidade sete dias após a data registrada.';


CREATE TABLE TBL_Ventania (
    Id     NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    Data   DATE NOT NULL,
    Dia_1  FLOAT(3),
    Dia_2  FLOAT(3),
    Dia_3  FLOAT(3),
    Dia_4  FLOAT(3),
    Dia_5  FLOAT(3),
    Dia_6  FLOAT(3),
    Dia_7  FLOAT(3)
);

COMMENT ON COLUMN TBL_Ventania.Id IS
    'Esta coluna irá receber o Id da Ventania registrada e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_Ventania.Data IS
    'Esta coluna irá receber o Id da Ventania registrada e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_Ventania.Dia_1 IS
    'Esta coluna irá receber a previsão da Ventania um dia após a data registrada.';

COMMENT ON COLUMN TBL_Ventania.Dia_2 IS
    'Esta coluna irá receber a previsão da Ventania dois dias após a data registrada.';

COMMENT ON COLUMN TBL_Ventania.Dia_3 IS
    'Esta coluna irá receber a previsão da Ventania três dias após a data registrada.';

COMMENT ON COLUMN TBL_Ventania.Dia_4 IS
    'Esta coluna irá receber a previsão da Ventania quatro dias após a data registrada.';

COMMENT ON COLUMN TBL_Ventania.Dia_5 IS
    'Esta coluna irá receber a previsão da Ventania cinco dias após a data registrada.';

COMMENT ON COLUMN TBL_Ventania.Dia_6 IS
    'Esta coluna irá receber a previsão da Ventania seis dias após a data registrada.';

COMMENT ON COLUMN TBL_Ventania.Dia_7 IS
    'Esta coluna irá receber a previsão da Ventania sete dias após a data registrada.';



CREATE TABLE TBL_Precipitacao (
    Id     NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    Data   DATE NOT NULL,
    Dia_1  FLOAT(3),
    Dia_2  FLOAT(3),
    Dia_3  FLOAT(3),
    Dia_4  FLOAT(3),
    Dia_5  FLOAT(3),
    Dia_6  FLOAT(3),
    Dia_7  FLOAT(3)
);

COMMENT ON COLUMN TBL_Precipitacao.Id IS
    'Esta coluna irá receber o Id da Precipitacao registrada e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_Precipitacao.Data IS
    'Esta coluna irá receber o Id da Precipitacao registrada e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_Precipitacao.Dia_1 IS
    'Esta coluna irá receber a previsão da Precipitacao um dia após a data registrada.';

COMMENT ON COLUMN TBL_Precipitacao.Dia_2 IS
    'Esta coluna irá receber a previsão da Precipitacao dois dias após a data registrada.';

COMMENT ON COLUMN TBL_Precipitacao.Dia_3 IS
    'Esta coluna irá receber a previsão da Precipitacao três dias após a data registrada.';

COMMENT ON COLUMN TBL_Precipitacao.Dia_4 IS
    'Esta coluna irá receber a previsão da Precipitacao quatro dias após a data registrada.';

COMMENT ON COLUMN TBL_Precipitacao.Dia_5 IS
    'Esta coluna irá receber a previsão da Precipitacao cinco dias após a data registrada.';

COMMENT ON COLUMN TBL_Precipitacao.Dia_6 IS
    'Esta coluna irá receber a previsão da Precipitacao seis dias após a data registrada.';

COMMENT ON COLUMN TBL_Precipitacao.Dia_7 IS
    'Esta coluna irá receber a previsão da Precipitacao sete dias após a data registrada.';


CREATE TABLE TBL_Nuvem (
    Id     NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    Data   DATE NOT NULL,
    Dia_1  VARCHAR2(50),
    Dia_2  VARCHAR2(50),
    Dia_3  VARCHAR2(50),
    Dia_4  VARCHAR2(50),
    Dia_5  VARCHAR2(50),
    Dia_6  VARCHAR2(50),
    Dia_7  VARCHAR2(50)
);

COMMENT ON COLUMN TBL_Nuvem.Id IS
    'Esta coluna irá receber o Id da Nuvem registrada e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_Nuvem.Data IS
    'Esta coluna irá receber o Id da Nuvem registrada e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_Nuvem.Dia_1 IS
    'Esta coluna irá receber a previsão da Nuvem um dia após a data registrada.';

COMMENT ON COLUMN TBL_Nuvem.Dia_2 IS
    'Esta coluna irá receber a previsão da Nuvem dois dias após a data registrada.';

COMMENT ON COLUMN TBL_Nuvem.Dia_3 IS
    'Esta coluna irá receber a previsão da Nuvem três dias após a data registrada.';

COMMENT ON COLUMN TBL_Nuvem.Dia_4 IS
    'Esta coluna irá receber a previsão da Nuvem quatro dias após a data registrada.';

COMMENT ON COLUMN TBL_Nuvem.Dia_5 IS
    'Esta coluna irá receber a previsão da Nuvem cinco dias após a data registrada.';

COMMENT ON COLUMN TBL_Nuvem.Dia_6 IS
    'Esta coluna irá receber a previsão da Nuvem seis dias após a data registrada.';

COMMENT ON COLUMN TBL_Nuvem.Dia_7 IS
    'Esta coluna irá receber a previsão da Nuvem sete dias após a data registrada.';


CREATE TABLE TBL_Previsao (
    Id              NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    Regiao_Id       NUMBER NOT NULL,
    Data            DATE NOT NULL,
    Temperatura_Id  NUMBER,
    Umidade_Id      NUMBER,
    Ventania_Id     NUMBER,
    Precipitacao_Id NUMBER,
    Nuvem_Id        NUMBER,
    FOREIGN KEY (Regiao_Id) REFERENCES TBL_Regiao(Id),
    FOREIGN KEY (Temperatura_Id) REFERENCES TBL_Temperatura(Id),
    FOREIGN KEY (Umidade_Id) REFERENCES TBL_Umidade(Id),
    FOREIGN KEY (Ventania_Id) REFERENCES TBL_Ventania(Id),
    FOREIGN KEY (Precipitacao_Id) REFERENCES TBL_Precipitacao(Id),
    FOREIGN KEY (Nuvem_Id) REFERENCES TBL_Nuvem(Id)
);

COMMENT ON COLUMN TBL_Previsao.Id IS
    'Esta coluna irá receber o Id da Previsao registrada e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_Previsao.Regiao_Id IS
    'Esta coluna irá receber o Id da Regiao registrada e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_Previsao.Data IS
    'Esta coluna irá receber a data atual e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_Previsao.Temperatura_Id IS
    'Esta coluna irá receber a previsão da temperatura relativa data atual.';

COMMENT ON COLUMN TBL_Previsao.Umidade_Id IS
    'Esta coluna irá receber a previsão da Umidade relativa data atual.';

COMMENT ON COLUMN TBL_Previsao.Ventania_Id IS
    'Esta coluna irá receber a previsão da Ventania relativa data atual.';

COMMENT ON COLUMN TBL_Previsao.Precipitacao_Id IS
    'Esta coluna irá receber a previsão da Precipitacao relativa data atual.';

COMMENT ON COLUMN TBL_Previsao.Nuvem_Id IS
    'Esta coluna irá receber a previsão de Nuvem relativa data atual.';


CREATE TABLE TBL_Previsao_Horaria (
    Id              NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    Regiao_Id       NUMBER NOT NULL,
    Data            DATE NOT NULL,
    Hora            NUMBER(2) NOT NULL,
    Temperatura     FLOAT(3),
    Umidade         FLOAT(3),
    Ventania        FLOAT(3),
    Precipitacao    FLOAT(3),
    Nuvem           VARCHAR2(50),
    FOREIGN KEY (Regiao_Id) REFERENCES TBL_Regiao(Id)
);

COMMENT ON COLUMN TBL_Previsao_Horaria.Id IS
    'Esta coluna irá receber o Id da Previsao registrada e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_Previsao_Horaria.Regiao_Id IS
    'Esta coluna irá receber o Id da Regiao registrada e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_Previsao_Horaria.Data IS
    'Esta coluna irá receber a data atual e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_Previsao_Horaria.Hora IS
    'Esta coluna irá receber a Hora atual e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_Previsao_Horaria.Temperatura IS
    'Esta coluna irá receber a previsão da temperatura relativa data atual.';

COMMENT ON COLUMN TBL_Previsao_Horaria.Umidade IS
    'Esta coluna irá receber a previsão da Umidade relativa data atual.';

COMMENT ON COLUMN TBL_Previsao_Horaria.Ventania IS
    'Esta coluna irá receber a previsão da Ventania relativa data atual.';

COMMENT ON COLUMN TBL_Previsao_Horaria.Precipitacao IS
    'Esta coluna irá receber a previsão da Precipitacao relativa data atual.';

COMMENT ON COLUMN TBL_Previsao_Horaria.Nuvem IS
    'Esta coluna irá receber a previsão de Nuvem relativa data atual.';


CREATE TABLE TBL_Historico_Meteorologico (
    Id              NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    Regiao_Id       NUMBER NOT NULL,
    Data            DATE NOT NULL,
    Hora            NUMBER(2) NOT NULL,
    Temperatura_Max FLOAT(3),
    Temperatura_Min FLOAT(3),
    Umidade_Max     FLOAT(3),
    Umidade_Min     FLOAT(3),
    Ventania        FLOAT(3),
    Precipitacao    FLOAT(3),
    Pressao_atm     FLOAT(3),
    Nuvem           VARCHAR2(50),
    FOREIGN KEY (Regiao_Id) REFERENCES TBL_Regiao(Id)
);

COMMENT ON COLUMN TBL_Historico_Meteorologico.Id IS
    'Esta coluna irá receber o Id da Previsao registrada e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_Historico_Meteorologico.Regiao_Id IS
    'Esta coluna irá receber o Id da Regiao registrada e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_Historico_Meteorologico.Data IS
    'Esta coluna irá receber a data registrada e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_Historico_Meteorologico.Hora IS
    'Esta coluna irá receber a Hora registrada e seu conteúdo é obrigatório.';

COMMENT ON COLUMN TBL_Historico_Meteorologico.Temperatura_Max IS
    'Esta coluna irá receber a temperatura máxima relativa registrada.';

COMMENT ON COLUMN TBL_Historico_Meteorologico.Temperatura_Min IS
    'Esta coluna irá receber a temperatura minima relativa registrada.';

COMMENT ON COLUMN TBL_Historico_Meteorologico.Umidade_Max IS
    'Esta coluna irá receber a Umidade máxima relativa registrada.';

COMMENT ON COLUMN TBL_Historico_Meteorologico.Umidade_Min IS
    'Esta coluna irá receber a Umidade minima relativa registrada.';

COMMENT ON COLUMN TBL_Historico_Meteorologico.Ventania IS
    'Esta coluna irá receber a Ventania relativa registrada.';

COMMENT ON COLUMN TBL_Historico_Meteorologico.Precipitacao IS
    'Esta coluna irá receber a Precipitacao relativa registrada.';

COMMENT ON COLUMN TBL_Historico_Meteorologico.Nuvem IS
    'Esta coluna irá receber a previsão de Nuvem relativa registrada.';
